interface:
  display_name: "Golang Samples"
  short_description: "Repo-specific patterns and workflows for golang-samples"
  default_prompt: "Use the golang-samples repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true